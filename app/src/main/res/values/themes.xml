<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DrawingApp" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <item name="colorPrimary" >@color/Blue</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.DrawingApp" parent="Base.Theme.DrawingApp" />

    <style name="Img_btn">
        <item name="android:shape">rectangle</item>
        <item name="android:solidColor">@color/black</item>
        <item name="android:strokeColor">#989898</item>
        <item name="android:radius">5dp</item>
        <item name="strokeWidth">5dp</item>
    </style>
</resources>